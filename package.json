{
	"name": "leadsheets",
	"version": "1.0.0",
	"description": "A collection of chordpro(ish) files",
	"main": "build.mjs",
	"scripts": {
    "prepare": "npm install",
		"build": "node .",
    "debug": "http-server files --cors=* -p 8000",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
  "dependencies": {
		"fast-glob": "^3.2.12"
  },
	"devDependencies": {
		"http-server": "^14.1.1"
	}
}
